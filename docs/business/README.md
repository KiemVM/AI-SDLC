# docs/business/

Reserved for a future Business phase pass (`/vision`, `/prd`, `/persona`, `/architecture`,
adapted from BMAD-METHOD's documentation-first governance). Empty by design — do not add
ad-hoc files here without extending `.claude/CONSTITUTION.md` and the command/skill set to
match.
